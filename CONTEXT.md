# AI Presentation Studio

AI Presentation Studio 帮助不懂 HTML/CSS 的人，把自己的中文产品项目材料制作成可轻量编辑的演示文稿。

## Language

**首个用户**：
不懂 HTML/CSS、需要把自己的产品项目材料制作成演示文稿的人。首个版本以产品作者本人为代表。
_Avoid_：泛知识工作者、所有非技术用户

**首个使用场景**：
首个用户输入一份中文产品项目材料，生成 HTML 演示文稿，修改其中的文字和图片位置，保存结果并导出 PDF。
_Avoid_：通用文档转换、完整演示文稿制作

**一周 MVP**：
在首个用户当前的 Windows 电脑上，使用一份提前准备的真实项目材料完成从双击启动、新建与生成，到文字和图片编辑、保存、重新打开及导出 HTML/PDF 的完整演示；5 个首发模板分别通过一次生成、打开、简单编辑和导出的基本检查，正式演示使用其中最稳定的模板。
_Avoid_：安装包、任意材料的全面兼容保证、面向其他用户的生产发布

**源材料**：
首个用户粘贴到单一文本框、用于生成演示文稿的中文文本内容；内容可以包含 Markdown 标记，但产品不区分输入模式。
_Avoid_：上传文件、Markdown 模式、纯文本模式

**演示模板**：
首个用户生成前选择的、来自 Open Design zhangzara 模板集合的生成预设；每个模板由模板提示词、示例 HTML 和产品统一生成约束共同组成，使 Codex 在保留特定视觉与版式体系的同时输出统一的可编辑结构。
_Avoid_：只有提示词的风格描述、用户自建模板、主题

**首发模板集**：
首个版本提供给用户选择的 5 个演示模板，均从 Open Design zhangzara 模板集合中筛选。
_Avoid_：完整模板库、自建模板集合

**生成请求**：
首个用户点击“发送”后提交源材料和所选演示模板，并立即启动的一次完整生成；用户不设置页数，生成过程不包含追问或继续对话。
_Avoid_：聊天消息、对话、澄清轮次

**可编辑文字**：
生成结果或导入演示中允许首个用户修改内容、字体、字号、颜色、粗细、行距、字间距和对齐方式，并可拖拽移动、复制和删除的现有文字元素。
_Avoid_：富文本编辑器、新增文字组件

**可编辑图片**：
生成结果或导入演示中允许首个用户拖动位置、通过四角控制点等比例改变尺寸和替换图片内容的现有内容图片；图片可以覆盖文字或其他内容，但必须完整保留在当前幻灯片范围内。首个版本只识别普通图片元素，不包含背景图、SVG、Logo 和模板装饰。
_Avoid_：自由拉伸、图片裁剪、新增图片、图片生成、背景和装饰编辑

**编辑历史**：
首个用户在当前编辑会话中对可编辑文字和可编辑图片进行的操作记录，可按顺序撤销或重做；重新打开项目后记录清空。
_Avoid_：生成历史、版本历史

**已保存状态**：
当前编辑结果已经成功写入本地的状态；后台自动保存或用户点击“保存”都可以进入该状态，主动保存成功后显示无需确认的短暂提示。
_Avoid_：项目完成、导出成功

**项目编辑状态**：
用于重新打开演示项目并继续编辑的结构化状态；它保存编辑器恢复所需的信息，并作为当前演示内容的内部依据。
_Avoid_：导出的 HTML、预览页面、撤销历史

**演示项目**：
由生成请求或导入演示创建的独立本地工作单元，包含当前演示内容和必要元数据。
_Avoid_：导出文件、源材料、文档

**项目名称**：
首页用于区分演示项目的名称，取源材料第一行，首个版本不可重命名。
_Avoid_：演示标题、文件路径

**项目删除**：
用户确认后永久移除应用内部演示项目的操作。
_Avoid_：回收站、批量删除

**导出成果**：
演示项目当前已保存版本生成的用户文件，包括可继续使用的自包含 HTML 和用于交付的 PDF；执行导出前先保存当前编辑。
_Avoid_：应用内部状态、PPTX

**重新生成**：
用户确认后，使用演示项目原有的源材料和演示模板重新执行生成请求，并覆盖当前工作 HTML 和已有编辑。
_Avoid_：新版本、版本历史、撤销生成

**项目状态**：
演示项目当前所处的用户可理解阶段；首个版本包含“生成中”“可编辑”“生成失败”和“已取消”。
_Avoid_：Codex 进程状态、内部错误码

**失败项目**：
生成请求失败后保留的演示项目，保存原源材料、所选模板和可读错误，并允许使用相同输入重试；不向用户展示不完整 HTML。
_Avoid_：失败日志、部分结果

**取消生成**：
用户停止当前 Codex 生成过程的操作；项目保留源材料和模板，进入“已取消”状态并允许重试，不展示未完成 HTML。
_Avoid_：删除项目、生成失败

**活动生成任务**：
当前唯一正在调用 Codex 的生成或重新生成任务；首个版本同时最多存在一个，运行期间其他演示项目仍可查看和编辑。
_Avoid_：生成队列、并行生成

**生成引擎**：
本机已经安装、登录并配置完成的 Codex CLI；产品使用其当前默认模型执行生成请求。
_Avoid_：模型提供商、API Key、产品内登录、模型选择器

**生成阶段**：
生成请求运行时向用户展示的业务进度，包括“正在准备材料”“正在生成演示文稿”“正在检查 HTML”和“生成完成”。
_Avoid_：Codex 事件、进程输出

**详细日志**：
默认折叠的技术运行记录，用于排查生成失败；它不是面向用户的主要进度信息。
_Avoid_：生成阶段、错误原因

**导入演示**：
用户上传一个自包含 HTML 演示文稿文件，经导入检查通过后创建演示项目的操作；第一版只接受单个文件上传，导入副本不含脚本，页面切换由产品提供的页面导航完成，编辑不改动原始上传文件。
_Avoid_：文件夹或压缩包上传、批量导入、直接编辑原文件

**导入检查**：
对上传 HTML 逐条规则评估能否安全打开并编辑的检查；结果分为「完全可编辑」「部分可编辑」和「暂不支持」三档，并生成导入检查报告。
_Avoid_：生成阶段检查、病毒扫描

**导入检查报告**：
导入检查后向用户展示的结果页，按判定档位逐条说明规则结果、可编辑内容、被锁定的内容、转为静态的动效和已移除的脚本，并可查看原始文件；用户确认后才进入编辑。
_Avoid_：技术日志、错误堆栈

**原始文件**：
用户上传的原始 HTML 演示文稿文件；导入检查和后续编辑都不改动它，导入检查报告的「查看原文件」入口展示的就是它的原样内容。
_Avoid_：导入副本、只读副本、导出成果

**锁定元素**：
导入检查判定为超出第一版编辑能力、但在演示中保留原样显示的元素；不可选中或修改，并在导入检查报告中列明。
_Avoid_：删除隐藏、报错中断

**动效转静态**：
导入演示时把上传文件中以样式规则声明的动画和过渡转为静止显示的处理；由脚本驱动的动效不属于此范围，相关内容按导入检查规则判定。
_Avoid_：动画编辑、时间轴调整

**脚本动效**：
导入文件里依赖原文件脚本驱动的动效（如逐帧动画、定时器动画）；导入时脚本被移除，这类动效冻结为静止画面，并在导入检查报告中按锁定元素列明。
_Avoid_：动画编辑、动效转静态（仅限样式规则声明的动画与过渡）

**安全预览**：
在受限环境中显示生成或导入的 HTML；允许视觉资源、动画和必要的内嵌翻页脚本，但阻止本地文件访问、主应用访问、页面跳转、新窗口、表单提交、下载和外部 API 调用。
_Avoid_：普通浏览器标签页、完全可信 HTML

**元素选择**：
用户直接点击画布中的可编辑文字或可编辑图片，并通过选中边框确认当前操作对象；不兼容元素只显示不可编辑提示。
_Avoid_：图层列表、DOM 树、容器选择

**预览模式**：
演示项目打开后的默认只读状态，用于浏览和翻页，不允许选择或修改元素。
_Avoid_：编辑画布、演示文件

**编辑模式**：
用户主动点击“编辑”后进入的状态，允许选择和修改兼容元素；点击“完成编辑”后返回预览模式。
_Avoid_：源码编辑、聊天编辑

**页面导航**：
用户通过“上一页”“下一页”、当前页码和键盘左右方向键在演示页面之间切换。
_Avoid_：页面缩略图、页面排序

**PDF 导出**：
保存当前编辑后，由本地后端按演示页面尺寸和分页规则直接生成并交付 PDF 文件的操作。
_Avoid_：浏览器打印窗口、PPTX 导出

**启动入口**：
首个版本用于双击启动本地后端并自动打开浏览器首页的 Windows 脚本；用户无需手动输入开发命令。
_Avoid_：安装包、桌面应用、手动 npm 命令

**运行窗口**：
启动入口打开并保持的最小化终端窗口，用于承载本地后端；关闭该窗口即退出应用并停止后端。
_Avoid_：系统托盘、后台服务、应用内退出
