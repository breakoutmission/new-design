---
status: superseded by ADR-0009
---
# 按能力检查 HTML 导入，不使用来源白名单

首个版本允许任何来源的本地 HTML 接受兼容性检查；五个内置模板只是预先验证的模板，不是导入白名单。HTML 只有在能够安全加载、稳定定位并保存编辑结果，且至少支持一种产品编辑能力时才允许导入；界面展示能力检查结果并禁用不支持的工具。这个选择扩大了可导入范围，但需要接受同一 HTML 只支持部分编辑能力的情况。
