# Open Design 仓库儿童版说明书

> 适合：没有代码经验，但懂一点 PRD（产品需求文档，也就是“要做什么、为什么做”的说明书）和 MVP（最小可行产品，也就是“先做最少但能完整使用的一版”）的人。
>
> 阅读方式：遇到专业名词不用查百度，我会在它后面立刻写一个括号解释。本文是普通 Markdown（像记事本一样的纯文字文件），不会打开交互网页。

## 先用一句话说清楚

Open Design 是一个开源仓库（大家可以查看源代码，并按许可证规则使用或修改的项目文件夹）。它把 Codex、Claude Code、Cursor 等 Coding Agent（会看文件、写代码、执行命令的 AI 助手）包装成一个比较容易使用的设计工作台，让人通过聊天生成网页、演示文稿、图片和视频。

你可以把它想成一家“AI 设计工厂”：

- 前台接待用户，收集“我要做什么”；
- 后台把要求交给 AI 工人；
- 工作说明书告诉 AI 工人应该怎样做；
- 品牌规则告诉它颜色、字体和间距应该长什么样；
- 最后把做好的文件放进作品柜，让用户预览和导出。

官方入口：

- [Open Design GitHub 仓库](https://github.com/nexu-io/open-design)
- [官方架构说明](https://github.com/nexu-io/open-design/blob/main/docs/architecture.md)
- [官方 Skill 规则](https://github.com/nexu-io/open-design/blob/main/docs/skills-protocol.md)

## 它不是一个“会画图的聊天框”

普通聊天产品只返回一段文字。Open Design 还会让 AI 在本地文件夹（你自己电脑上的文件夹）中创建真实文件，例如：

- HTML（浏览器能打开的网页文件）；
- PPTX（PowerPoint 演示文稿文件）；
- PDF（排版固定、方便查看和分享的文档文件）；
- MP4（常见的视频文件）；
- 图片文件。

这些生成出来的真实作品，在 Open Design 中叫 Artifact（AI 实际做出来并保存到硬盘上的作品文件）。

因此，它真正有价值的地方不是“能聊天”，而是把下面这条完整链路连了起来：

```text
用户说需求
  ↓
AI 理解任务
  ↓
AI 按说明书创建文件
  ↓
用户预览和提出修改
  ↓
AI 修改原文件
  ↓
导出最终作品
```

## 一次生成任务到底发生了什么

假设你说：“帮我做一份咖啡品牌路演 PPT。”Open Design 大致会这样工作：

1. Web App（在浏览器中看到和点击的产品页面）收下你的文字、文件和选择。
2. Daemon（一直藏在电脑后台干活的小助手）创建一个任务，找到你选择的 Skill（教 AI 完成某一种工作的说明书）。
3. Daemon（后台小助手）启动 Codex 或 Claude Code 之类的 Agent（会自己拆步骤、读文件并使用工具的 AI 助手）。
4. Agent（AI 助手）读取 Skill（工作说明书）、Design System（规定颜色、字体、按钮和间距的品牌装修规则）以及 Template（提前做好基本结构的半成品）。
5. Agent（AI 助手）在本地文件夹中生成 Artifact（真实作品文件）。
6. Preview（在正式导出前先看一眼作品的预览区）把网页或幻灯片显示出来。
7. 你通过评论、滑块或手动检查器提出修改，Agent（AI 助手）继续修改原文件。
8. Export Pipeline（把作品加工成 PPTX、PDF、MP4 等格式的一条流水线）输出最终文件。

最重要的一句话：**Open Design 自己主要负责“组织工作”，真正写作品的是它启动的 Agent（AI 助手）。**

## 五层结构：把它看成一栋五层楼

### 第一层：用户看到的页面

这层主要在 `apps/web` 文件夹。

它包含：

- Chat Pane（让用户和 AI 对话的聊天区域）；
- Artifact Tree（像文件资源管理器一样列出所有作品文件的目录树）；
- Iframe Preview（网页中的一个透明小盒子，用来安全地展示另一个网页）；
- Comment Overlay（盖在作品上方、用来标记问题的透明评论层）；
- Slider（拖动后改变数值的小横条，例如把圆角从 8 调到 16）。

这层解决的问题是：**不会用命令行的人，怎样也能把需求交给 AI 并看到结果。**

命令行（在黑色或蓝色文字窗口中输入命令来控制电脑）对开发者很方便，对普通用户却很吓人，所以产品需要一个更简单的页面。

### 第二层：藏在电脑后面的调度员

这层主要在 `apps/daemon` 文件夹。

Daemon（一直在后台运行、替前台做重活的小程序）负责：

- 管理 Session（一次从开始到结束的任务记录，像一张不会丢的作业单）；
- 寻找和读取 Skill（AI 工作说明书）；
- 启动不同的 Agent（AI 助手）；
- 保存 Artifact（作品文件）；
- 编译 Preview（把源文件加工成浏览器能展示的样子）；
- 执行 Export（把作品另存成用户需要的文件格式）；
- 把进度不断送回前台。

这里的“后台”不是云端服务器（放在别人的机房里、通过互联网使用的电脑），而可以是你自己电脑上运行的本地服务（只在自己电脑上提供能力的小程序）。这就是 Local-first（尽量在用户自己的电脑上处理文件，而不是先传到别人的服务器）。

### 第三层：真正干活的 AI 助手

Open Design 支持 Codex、Claude Code、Cursor、Gemini、OpenCode 等 Agent（会操作文件和工具的 AI 助手）。

不同 Agent（AI 助手）讲话和做事的方式不完全一样，所以仓库里需要 Agent Adapter（像翻译员一样，把不同 AI 助手接成同一种用法）。官方架构文档把相关入口指向 `apps/daemon/src/agents.ts`。

你不需要一开始就支持很多 Agent（AI 助手）。你的 DeckFlow Lite（你准备做的轻量 PPT 产品）第一版只接 Codex，反而更容易完成和稳定。

### 第四层：教 AI 怎样工作

这一层最容易混淆，先看四种“积木”的区别：

| 名称 | 10 岁小孩版解释 | 它回答的问题 | Open Design 中的大致位置 |
|---|---|---|---|
| Skill（特定任务的工作说明书） | 像“怎样做蛋糕”的步骤卡 | 这项工作应该怎么做？ | `skills/` |
| Design System（品牌装修规则） | 像“我们家只能用蓝色墙和圆角家具”的规定 | 做出来应该长什么样？ | `design-systems/` |
| Template（预先做好的半成品） | 像已经搭好墙的积木房子，只需换内容 | 从什么结构开始做？ | `design-templates/`、`templates/` |
| Plugin（装上后增加一组能力的功能包） | 像给玩具车插上一套新的消防车零件 | 怎样一次增加一整组功能？ | `plugins/` |

还有 Prompt Template（可以重复使用的提问范本），像填空题：“为【受众】制作一份关于【主题】的【页数】页演示文稿。”它主要放在 `prompt-templates/`。

你的 `ppt-master` 就属于 Skill（教 AI 如何制作高质量 PPT 的工作说明书），不是 Template（提前做好的半成品），也不是 Agent（真正执行任务的 AI 助手）。

### 第五层：作品、预览和导出

Agent（AI 助手）会创建 HTML（网页文件）、SVG（放大不会变糊的图形文件）或其他中间文件。Preview Pipeline（把中间文件加工成可预览内容的一条流水线）负责显示它们，Export Pipeline（把作品变成最终格式的一条流水线）负责输出 PPTX、PDF 或 MP4。

这层解决的问题是：**AI 说“我做完了”不算完成，用户必须真的拿到可以打开、修改或分享的文件。**

## 仓库主要文件夹地图

Open Design 是 Monorepo（一个大仓库里同时装着多个应用和公共零件）。你不需要从头到尾读完，只需要认识下面这些地方。

### 你最应该先看的

| 文件夹 | 儿童版说明 | 对你有什么用 |
|---|---|---|
| `apps/web` | 用户看到的网页前台 | 学习上传、聊天、任务列表、作品预览怎样组成产品 |
| `apps/daemon` | 藏在后台的调度员 | 学习怎样启动 AI、恢复任务、保存产物和返回进度 |
| `skills` | 一排工作说明书 | 学习怎样把 `ppt-master` 变成产品能力 |
| `design-systems` | 一排品牌装修规则 | 学习颜色、字体、间距怎样被重复使用 |
| `design-templates` | 一排设计半成品 | 挑少量模板放进你的第一版产品 |
| `docs` | 项目说明书图书馆 | 先读架构和 Skill 规则，不用猜代码 |

### 知道存在，但第一周不用细看

| 文件夹 | 儿童版说明 |
|---|---|
| `apps/desktop` | 把产品装成电脑软件的外壳 |
| `apps/packaged` | 为发布安装版准备的打包应用 |
| `apps/landing-page` | 向别人介绍产品的宣传网站 |
| `packages` | 多个应用共同使用的公共零件箱 |
| `plugins` | 可以额外装上的成套功能包 |
| `assets` | 图片、图标、字体等素材箱 |
| `craft` | 与作品制作流程有关的工具和资源 |
| `specs` | 对某项功能应该怎样工作的详细约定 |
| `e2e` | 模拟用户从头点到尾的自动检查 |
| `mocks` | 测试时使用的假数据和假服务 |
| `scripts` | 帮开发者自动做重复工作的短程序 |

E2E Test（让电脑假装用户，从开始到结束自动点一遍的检查）和 Mock（测试时用来代替真实数据或真实服务的替身）都很重要，但不是你理解产品结构的第一站。

## Skill 文件里面有什么

Open Design 的 Skill（AI 工作说明书）通常是一个文件夹，里面可能包含：

```text
一个-skill/
├── SKILL.md
├── assets/
└── references/
```

- `SKILL.md`：主说明书，告诉 AI 目标、步骤和规则；
- `assets/`：做任务时可以拿来使用的图片、字体或模板素材；
- `references/`：给 AI 阅读的参考资料。

`SKILL.md` 最前面还可能有 YAML Frontmatter（写在文件顶部、像身份证一样记录名称和设置的小区域）。Open Design 在里面增加了 `od:` 设置，例如：

- `mode`（这个工作属于网页、幻灯片还是设计系统）；
- `preview`（怎样显示预览）；
- `inputs`（用户需要填写或上传什么）；
- `parameters`（用户可以调整哪些数值，例如间距或圆角）；
- `outputs`（最终应该产生哪些文件）；
- `capabilities`（完成任务需要哪些能力）。

这对你的产品非常关键，因为它说明：**一个 Skill（AI 工作说明书）不只可以教 AI 做事，还可以告诉产品页面“应该向用户收集什么、允许用户调什么、最后展示什么”。**

## 三种运行方式

官方架构文档描述了三种 Topology（软件各部分放在哪里运行的组合方式）。不用记英文，只看区别：

1. 全部在本机：网页前台、Daemon（后台小助手）和 Agent（AI 助手）都在用户电脑上。文件最容易保留在本地。
2. 网页在 Vercel（帮开发者托管网站的网络平台），Daemon（后台小助手）在用户电脑上，两边通过 Tunnel（像一条临时秘密通道，把外面的网页接到本机服务）连接。
3. 网页直接调用模型 API（让两个软件按照约定互相传话的接口），不使用本地 Daemon（后台小助手）。这种方式更容易放到网上，但会失去一部分本地文件、Skill 和 PPTX 导出能力。

对你的第一版来说，第一种最合适：**本地网页 + 本地后台 + 本地 Codex。**它不需要先解决账号、云端存储和服务器费用。

## Open Design 现在有没有手动修改能力

有一部分，不能再简单地说“完全不能改”。当前仓库已经出现：

- Slider（拖动后修改数值的小横条）；
- Tweak（对颜色、圆角、间距等做的小调整）；
- Manual Edit Inspector（点选作品中的元素，再手动检查和修改属性的工具）；
- Focus Mode（暂时隐藏干扰内容、专心看作品的模式）；
- Remove Element Patch（删除作品中某个元素的小修改指令）。

这些能力是否稳定、是否覆盖所有作品，要以你实际运行的版本为准。官方更新记录见 [CHANGELOG](https://github.com/nexu-io/open-design/blob/main/CHANGELOG.md)。

但你已经决定第一版不做可视化微调，这是正确的范围控制。可视化编辑器（可以直接拖动、缩放和修改画面元素的工具）看起来只是“拖一下”，背后却需要处理选择、对齐、撤销、保存、冲突和多种文件结构，很容易把项目拖大。

## 你的轻量产品应该借什么

不是把 Open Design 整个复制一份，而是借它最有价值的产品结构。

### 第一版可以借

1. **前台—后台—Agent 的三层分工**：页面只负责收集信息和展示进度，后台负责启动 Codex，Codex 负责执行 `ppt-master`。
2. **Skill 是产品能力合同**：页面从 `ppt-master` 的要求中整理输入项、确认项和输出项。
3. **Artifact 是完成标准**：必须展示真实 PPTX、预览文件和产物目录，不能只显示一句“生成成功”。
4. **Session 是任务记忆**：用户确认八项建议后，恢复同一个任务继续生成，不重新开始。
5. **模板只选少量**：第一版放 3 个差异明显的模板已经足够证明选择逻辑。

### 第一版不要借

- 多 Agent 切换；
- Plugin 市场（让用户浏览和安装很多功能包的商店）；
- 云端账号和同步；
- 视频、图片、网页等多种生成模式；
- 完整设计系统编辑器；
- 拖拽式可视化编辑；
- 团队协作和权限系统。

这些不是坏功能，只是不服务于你当前的目标：**尽快独立做出一条完整、稳定、可以面试演示的 PPT 生成流程。**

## 你的 DeckFlow Lite 与 Open Design 的关系

可以这样向面试官解释：

> 我研究了 Open Design 如何把通用 Coding Agent（会写代码和操作文件的 AI 助手）包装成垂直设计产品。我没有复制它的全部功能，而是提炼了“本地后台调度、Skill 驱动流程、人工确认、任务恢复、真实产物管理”五个关键机制，做成一个只服务 PPT 生成的轻量产品。第一版只支持 Codex 和 `ppt-master`，用范围控制换取完整性和可演示性。

这句话能体现三种产品经理能力：

- 竞品拆解（把别人的产品分成用户问题、流程和模块来研究）；
- 范围控制（明确第一版做什么和不做什么）；
- 技术理解（不一定亲手写所有代码，但知道每一层负责什么、怎样连起来）。

## 不要直接复制整个仓库

原因有三个：

1. Open Design 的范围很大，你会花大量时间安装和理解与你的 MVP（最小但完整可用的一版）无关的部分。
2. 它使用 Apache-2.0 License（允许使用和修改，但需要遵守保留许可证等条件的开源规则）；仓库中某些模板或外部资源还可能有自己的 License（别人允许你怎样使用作品的法律说明），复制前要逐个检查。
3. 面试官更想看到你的取舍和独立判断，而不是一个只改了名字的大项目。

最好的方法是：**参考产品结构，自己做窄；需要使用具体代码或模板时，再检查对应文件的许可证和署名要求。**

## 建议的仓库学习顺序

不要从第一行代码开始读。按下面顺序，每一步只回答一个问题：

### 第 1 步：读 README

打开 [README](https://github.com/nexu-io/open-design#readme)，只回答：它帮助谁完成什么任务？它有哪些生成模式？它承诺哪些导出结果？

### 第 2 步：读架构说明

打开 [architecture.md](https://github.com/nexu-io/open-design/blob/main/docs/architecture.md)，只画出“网页前台 → 后台小助手 → AI 助手 → 作品文件”四个方框。

### 第 3 步：看一个 Skill

进入 [`skills/`](https://github.com/nexu-io/open-design/tree/main/skills)，只选一个和演示文稿有关的 Skill（AI 工作说明书），找出它的输入、步骤、确认点和输出。

### 第 4 步：看一个 Design System 和一个 Template

分别进入 [`design-systems/`](https://github.com/nexu-io/open-design/tree/main/design-systems) 和 [`design-templates/`](https://github.com/nexu-io/open-design/tree/main/design-templates)，比较“视觉规则”和“现成结构”有什么不同。

### 第 5 步：回到自己的 PRD

把看到的机制分成三栏：

```text
P0：没有它就走不通完整流程
P1：流程走通后再做会明显更好
不做：对当前求职作品没有足够价值
```

你的 P0（第一版必须有的最高优先级功能）仍然应该是：上传材料、填写要求、启动 Codex、展示八项确认、恢复任务、展示真实产物和历史记录。

## 10 岁小孩也能回答的检查题

看完后，你应该能用自己的话回答：

1. Open Design 自己负责组织工作，真正创建文件的是谁？
2. Skill（AI 工作说明书）和 Template（设计半成品）有什么不同？
3. 为什么需要 Daemon（藏在后台的小助手）？
4. Artifact（真实作品文件）为什么比一句“生成成功”更重要？
5. 为什么 DeckFlow Lite 第一版只接一个 Agent（AI 助手）？

参考答案：AI Agent；Skill 讲方法而 Template 提供起点；后台小助手负责启动任务、保存文件和返回进度；用户最终需要可打开的真实文件；只接一个更容易快速做完整、做稳定。

## 最后只记住这张小抄

```text
Open Design = 设计任务的总工厂
Web App      = 用户看见的前台
Daemon       = 藏在本机的调度员
Agent        = 真正干活的 AI 工人
Skill        = 工作步骤说明书
Design System= 品牌装修规则
Template     = 已经搭好的半成品
Artifact     = 真正生成并保存的作品文件
Export       = 把作品加工成 PPTX/PDF/MP4
```

对你的产品来说，不需要复制整座工厂。先做一条能稳定生产 PPT 的小流水线，就是一个完整而可信的 MVP（最小但完整可用的一版）。
