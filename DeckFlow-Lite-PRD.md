# DeckFlow Lite PRD

## AI 速读卡

- 产品一句话：把 `ppt-master` 的复杂 Agent 流程包装成非技术用户可操作的 Windows 本地网页产品。
- 核心循环：上传材料 → 启动生成 → 查看八项建议 → 确认/修改 → 恢复任务 → 获取 PPTX。
- 目标平台：Windows 本机，浏览器访问 localhost。
- 硬约束：只允许 `ppt-master`；任务使用独立目录；不得绕过 Codex 沙箱；必须保留确认门。
- 推荐默认：React/Vite 前端 + Node 服务 + SSE + JSON 文件存储。
- 发挥空间：视觉语言、进度动画、空状态和产物卡片。
- P0 验收：真实材料可完成两阶段 Codex 任务并产出可打开的 PPTX。
- 最容易翻车：错误解析 Codex JSONL、丢失 session/thread id、把确认门误做成自动继续。
- 超预期机会：把 Agent 日志翻译为用户能懂的阶段进度，并生成一次任务复盘卡。

## 第一章：产品概述

DeckFlow Lite 是一款本地 Skill 运行器，让不熟悉命令行的内容创作者和产品经理能够通过表单完成专业 PPT 生成，而无需理解 Codex 命令、Skill 文件或产物目录。

### 1.1 差异化对比表

| 功能 | Open Design / 通用 Agent 界面 | DeckFlow Lite | 实现方式 |
|---|---|---|---|
| 任务入口 | 覆盖多种设计任务和 Agent | 只有“创建 PPT”一条入口 | 固定 `ppt-master` 任务定义 |
| Skill 交互 | 通用对话与产物面板 | 将八项确认显式做成产品阶段 | 运行后暂停，用户确认，再 resume |
| 运行信息 | 面向熟悉 Agent 的用户 | 只显示用户能理解的 PPT 阶段 | JSONL 事件映射为 6 个业务状态 |
| 文件范围 | 可处理多种项目和插件 | 每个 PPT 任务只能写自己的目录 | 服务端创建并校验任务工作区 |

说明：Open Design 的具体最新能力会继续变化；上表只用于确定本产品的范围策略，不宣称其缺少某一能力。

### 1.2 三类用户画像

| 角色 | 核心目标 | 对现有工具最大的不满 | 愿意切换的一个功能 |
|---|---|---|---|
| 初级产品经理 | 把研究材料快速变成汇报 PPT | 不知道怎样向 Agent 交代完整设计要求 | 表单提交后自动进入正确 Skill 流程 |
| 教师/研究者 | 把论文、Word 或 PDF 变成讲稿 | 命令行、路径和中间文件难理解 | 上传材料后集中查看 PPTX 和过程文件 |
| 高频 AI 创作者 | 重复运行 ppt-master 并保留历史 | 每次都要重复描述、查找目录和恢复任务 | 任务历史与一键继续 |

### 1.3 可行性边界

| 在范围内（及原因） | 明确排除在外（及原因） |
|---|---|
| Windows 本机运行：当前环境已验证 `codex exec` 可调用 | 公网 SaaS：服务依赖本机 Codex 登录态和文件系统 |
| `ppt-master` 单 Skill：能形成一条完整产物链 | 多 Skill 市场：会引入不同输入、状态和输出协议 |
| PDF、DOCX、Markdown 等 Skill 已支持的来源 | 任意云盘抓取：需要账号、网络与授权处理 |
| 两阶段任务：首次运行 + 用户确认后 resume | 自动跳过八项确认：违反 Skill 的强制工作流 |
| 本地任务历史与产物索引 | 多设备同步、团队协作、付费与账号 |
| 后续通过适配器加入少量模板 | 第一版直接兼容全部 Open Design HTML 模板：格式体系不同 |

### 1.4 约束分层

| 硬约束 | 推荐默认 | 发挥空间 |
|---|---|---|
| 仅运行白名单 Skill；任务目录不得越过产品数据根目录 | Windows-first、本地网页、不做桌面打包 | 首页和运行页的视觉风格 |
| 保存 session/thread id 后才能允许继续 | 使用 SSE 推送事件，JSON 文件保存任务元数据 | 阶段图标、动效、用户文案 |
| 八项确认必须由用户明确提交 | 原样保留完整 Codex 最后消息，同时显示简化阶段 | 日志折叠、任务复盘卡 |
| 不使用危险的 bypass sandbox 参数 | 默认 `workspace-write`，每次任务独立工作区 | 产物卡片排序与预览样式 |
| 产物存在且可打开才标记成功 | P0 只支持一个并发任务 | 空状态和新手提示 |

## 第二章：整体布局与导航

```text
+--------------------------------------------------------------------------------+
| DeckFlow Lite | 新建 PPT | 任务历史 | 环境状态：Codex 已连接                 |
+----------------------+---------------------------------------------------------+
| 左侧导航 220px       | 主内容区                                                |
| + 新建 PPT           | 首页：最近任务 / 新建入口                              |
| 历史任务             | 新建：材料与基本信息表单                               |
| 环境检查             | 运行：阶段、Agent 消息、确认区、产物                   |
| 关于                 |                                                         |
+----------------------+---------------------------------------------------------+
```

固定左侧导航帮助用户持续知道自己处于新建、运行还是结果阶段。运行页不使用通用聊天双栏，避免用户误以为需要自由提示工程。

```text
新建 PPT → 上传材料 → 首次 Codex 运行
                         |-- 环境/进程失败 → 保留材料与日志 → 重试
                         v
                    等待八项确认
                         |-- 用户确认或修改 → resume 原会话
                         |-- session 缺失 → 重新运行或人工恢复
                         v
                    校验 PPTX → 展示产物
```

## 第三章：核心模块详细设计

### 第 3.1 节 环境检查与新建任务

#### a) ASCII 图

```text
+-------------------------------------------------------------+
| 创建 PPT                                                    |
| 环境：Codex 0.144.1 〈已连接〉  ppt-master 〈已找到〉          |
| 演示标题      AI 产品经理项目复盘                            |
| 目标受众      互联网产品岗位面试官                           |
| 演示时长      6 分钟                 建议页数 8–10 页         |
| 材料          resume-project.pdf      〈重新选择〉             |
| 补充要求      中文、突出需求取舍和失败迭代                   |
|                                      〈创建并开始分析〉        |
+-------------------------------------------------------------+
```

空状态显示“选择 PDF、DOCX 或 Markdown 开始”；环境异常时提交按钮禁用，并显示“重新检查环境”。

#### b) 交互流程

```text
打开新建页 → 检查 codex 与 ppt-master → 填表 → 提交 → 创建任务目录
                    |                    |
                    |                    +-- 文件复制失败 → 保留表单 → 重新选择
                    +-- 命令/Skill 缺失 → 禁止提交 → 显示修复说明
```

#### c) 状态清单

| 名称 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 检查中 | 页面首次打开 | 灰色旋转标识 | 两项环境检查完成 |
| 可创建 | Codex 与 Skill 均存在 | 绿色环境状态 | 用户提交或环境变化 |
| 表单错误 | 必填项缺失/文件不支持 | 字段下红色说明 | 字段合法 |
| 环境错误 | 命令或 Skill 不存在 | 红色状态卡 | 重新检查成功 |
| 创建中 | 正在复制材料与写元数据 | 按钮加载 | 任务创建或失败 |

#### d) 依赖关系

读取环境检查 API、Skill 白名单和允许的文件扩展名；写入任务元数据、材料副本和初始 prompt。

#### e) 待决问题

1. 浏览器上传大文件的上限尚未通过真实材料验证，P0 默认 100MB。
2. 第一版默认只做上传，不直接输入任意本地绝对路径，减少权限问题。

### 第 3.2 节 Agent 运行与阶段映射

#### a) ASCII 图

```text
+-------------------------------------------------------------+
| AI 产品经理项目复盘                          〈停止任务〉      |
| ● 材料处理  ● 策略分析  ◐ 等待设计确认  ○ 页面生成  ○ 导出   |
| 当前状态：正在整理八项设计建议                              |
| 最近消息：已读取 54 页材料，建议生成 9 页……                 |
| 〈查看完整 Agent 日志 ▾〉                                    |
+-------------------------------------------------------------+
```

#### b) 交互流程

```text
spawn codex exec --json → 逐行解析 JSONL → SSE 推送 → 更新阶段
               |                                  |
               +-- 非 JSON 行 → 记录 raw 日志，不中断任务
               +-- 异常退出 → failed，展示 exit code 与末尾日志
```

#### c) 状态清单

| 名称 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 排队 | 已有运行任务 | 空心时钟 | 获得执行槽 |
| 运行中 | Codex 进程存在 | 蓝色阶段动画 | 等待确认、成功、失败或停止 |
| 等待确认 | 首次进程结束、有 session id、无 PPTX | 橙色确认卡 | 用户提交确认或修改 |
| 恢复中 | 已调用 exec resume | 蓝色恢复提示 | 新事件到达或失败 |
| 已停止 | 用户点击停止 | 灰色停止标识 | 用户继续或重试 |
| 失败 | 异常退出或无法启动 | 红色错误卡 | 重试或继续成功 |

#### d) 依赖关系

读取任务 prompt、工作目录和 Codex 可执行文件；写入 JSONL 日志、session/thread id、阶段、exit code 和时间戳。

#### e) 待决问题

1. `codex exec --json` 的事件字段必须用当前本机版本做技术探针后固化。
2. CLI 升级后未知事件默认落盘并忽略，不得让任务崩溃。

### 第 3.3 节 八项确认与任务恢复

#### a) ASCII 图

```text
+-------------------------------------------------------------+
| 请确认设计方向                                              |
| Agent 完整建议                                              |
| 1. 画布：16:9                                               |
| 2. 页数：8–10 页                                            |
| 3. 受众：产品岗位面试官                                     |
| ...                                                         |
| 修改意见（可选）                                            |
| 「第 5 项改为蓝灰色；图片减少，优先图表」                   |
| 〈确认原建议并继续〉                 〈按修改意见继续〉       |
+-------------------------------------------------------------+
```

#### b) 交互流程

```text
首次运行结束 → 保存 session id → 展示完整建议 → 用户提交
       |                                  |
       +-- 无 session id → 禁止继续，允许重新运行
       +-- resume 失败 → 保留意见，显示重试
                                          v
                         codex exec resume <id> <确认文本>
```

#### c) 状态清单

| 名称 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 待确认 | 首次阶段结束 | 橙色边框 | 用户提交 |
| 意见编辑中 | 文本框有内容 | 字数与保存提示 | 提交或清空 |
| 提交中 | 正在调用 resume | 按钮禁用 | resume 启动或失败 |
| 恢复失败 | session 无效/进程失败 | 原因与重试 | 重试成功或重建任务 |
| 已确认 | resume 已启动 | 只读确认记录 | 任务完成 |

#### d) 依赖关系

读取 session/thread id 与首次最后消息；写入用户确认文本、确认时间、resume 次数和新事件日志。

#### e) 待决问题

1. P0 不把八项建议解析为八个字段，只展示完整建议并允许整体修改，以缩短开发时间。
2. 同一任务允许多次 resume，但界面只突出第一次设计确认。

### 第 3.4 节 产物与任务历史

#### a) ASCII 图

```text
+-------------------------------------------------------------+
| 生成完成 · 用时 18 分 42 秒                                 |
| PPTX：AI产品经理项目复盘.pptx        12.8 MB   〈下载〉       |
| PDF：AI产品经理项目复盘.pdf           4.3 MB   〈下载〉       |
| 目录：SVG、讲稿与设计规范                     〈打开目录〉   |
| 〈查看运行复盘〉 〈继续修改此任务〉 〈返回任务历史〉          |
+-------------------------------------------------------------+
```

#### b) 交互流程

```text
Codex 结束 → 扫描白名单扩展名 → 校验 PPTX 存在且大小 > 0 → 成功
                 |                         |
                 +-- 无 PPTX → 未完成，允许继续任务
                 +-- 文件被占用 → 提示关闭占用并重试
```

#### c) 状态清单

| 名称 | 触发条件 | 视觉标识 | 退出条件 |
|---|---|---|---|
| 扫描产物 | 进程结束 | 灰色扫描提示 | 清单生成 |
| 成功 | 存在有效 PPTX | 绿色完成卡 | 用户继续任务 |
| 未完成 | 无 PPTX 但有项目文件 | 橙色继续卡 | resume 或重试 |
| 下载失败 | 文件不存在/被占用 | 红色行内提示 | 重新扫描/重试 |
| 历史空状态 | 无任务 | “创建第一份 PPT” | 创建任务 |

#### d) 依赖关系

读取任务目录与允许的产物扩展名；写入产物索引、文件大小、修改时间和最终状态。

#### e) 待决问题

1. P0 只提供文件下载和打开目录，不在浏览器内渲染完整 PPTX。
2. PDF 为可选产物；没有 PDF 不影响 PPTX 成功标准。

## 第四章：超越竞品的差异化功能

### 第 4.1 节 把 Agent 技术日志翻译成 PPT 阶段

1. **竞品为何没有这个功能**：基于公开结构的推断，通用 Agent 产品要承载多种任务，因此通常保留通用消息与工具事件，不会为单一 `ppt-master` 维护强业务阶段。
2. **本产品如何实现**：保留 raw JSONL；另外维护材料处理、策略分析、等待确认、页面生成、质量检查、导出六阶段。映射失败时退回“Agent 正在工作”，不伪造百分比。
3. **交互流程**：

```text
Codex 事件 → 关键词/文件变化/进程状态 → 业务阶段
     |              映射未知              |
     +-----------------------------------> “Agent 正在工作”
```

4. **风险与应对**：Skill 文案升级可能使关键词失效；以文件产物和进程状态作为更稳定信号，并保留未知回退。

### 第 4.2 节 可恢复的人工确认门

1. **竞品为何没有这个功能**：通用表单生成器倾向一次提交到底，而 `ppt-master` 刻意要求先给建议、再取得明确确认，这是 Skill 的治理要求。
2. **本产品如何实现**：保存 session/thread id 与首次最后消息，用户确认后用 `codex exec resume` 恢复，不重新创建上下文。
3. **交互流程**：

```text
Phase A → 展示建议 → 用户确认/修改 → resume 原会话 → Phase B → PPTX
```

4. **风险与应对**：session 失效时保留项目目录和意见，提供基于现有项目启动新任务的降级入口。

### 超预期机会

- 阶段翻译：把技术事件变成“正在整理材料”“等待你确认设计方向”。
- 任务复盘卡：汇总材料数、确认修改、运行时长、失败与产物。
- 安全失败：明确告诉用户材料已保留，不必重新上传。

## 第五章：数据模型

```jsonc
{
  "version": "1.0", // 数据结构版本，必填
  "id": "job_20260714_001", // 任务唯一标识，必填
  "title": "AI 产品经理项目复盘", // 用户可见标题，必填
  "status": "awaiting_confirmation", // queued|running|awaiting_confirmation|resuming|completed|failed|stopped，必填
  "stage": "strategy", // intake|source|strategy|confirmation|generation|quality|export，必填
  "createdAt": "2026-07-14T19:30:00+08:00", // ISO 8601 创建时间，必填
  "updatedAt": "2026-07-14T19:42:00+08:00", // ISO 8601 更新时间，必填
  "workspacePath": "G:/AITOOLS/NEW-DESIGN/data/jobs/job_20260714_001", // 必须位于数据根目录
  "input": { // 用户输入
    "audience": "互联网产品岗位面试官", // 目标受众，必填
    "durationMinutes": 6, // 演示分钟数，1-120，必填
    "notes": "中文，突出需求取舍", // 补充要求，默认值: 空字符串
    "sourceFiles": { // 已复制材料的文件映射，至少一个
      "resume-project.pdf": 1048576 // 文件大小，单位 bytes，必须大于 0
    }
  },
  "agent": { // Codex 执行信息
    "command": "codex exec", // 只用于审计，不允许用户编辑
    "sessionId": "019f...", // 首次事件中获得；确认前可能为空
    "exitCode": 0, // 最近一次退出码；运行中为 null
    "resumeCount": 0, // 恢复次数，默认值: 0
    "lastMessageFile": "logs/last-message.md", // 最后消息相对路径
    "eventLogFile": "logs/events.jsonl" // 原始事件日志相对路径，必填
  },
  "confirmation": { // 八项确认记录
    "proposalFile": "logs/phase-a-proposal.md", // 首次建议相对路径
    "userResponse": "确认以上八项，继续生成", // 用户明确提交的文本
    "confirmedAt": "2026-07-14T19:45:00+08:00" // 未确认时为 null
  },
  "artifacts": { // 仅索引白名单文件
    "primary": { // 主产物，P0 为 PPTX
      "name": "AI产品经理项目复盘.pptx", // 文件名，必填
      "type": "pptx", // pptx|pdf|svg|markdown|image，必填
      "relativePath": "output/AI产品经理项目复盘.pptx", // 不允许绝对路径或 ..
      "sizeBytes": 13421772 // 文件字节数，必须大于 0
    }
  },
  "error": { // 无错误时为 null
    "code": "CODEX_EXIT_NONZERO", // 稳定错误码
    "message": "Codex 异常退出，材料和日志已保留", // 用户可见说明
    "recoverable": true // 是否展示继续/重试
  }
}
```

任务状态、Agent 会话和产物索引分开。P0 不保存账号、聊天数据库或模板市场数据；完整事件只写入任务目录 JSONL。

## 第六章：技术架构

```text
+----------------------- 浏览器 UI ---------------------------+
| 任务表单、阶段状态、确认输入、历史与产物下载                |
+--------------------------|----------------------------------+
                           | HTTP + SSE
+--------------------------v----------------------------------+
| Node 本地服务                                               |
| 校验输入与边界 / 保存任务 / 启动 Codex / 解析 JSONL         |
| 保存 session id / 推送事件 / 扫描白名单产物                 |
+---------------------|-------------------|-------------------+
                      |                   |
             +--------v--------+  +-------v----------------+
             | Codex CLI       |  | data/jobs/<job-id>/    |
             | exec / resume   |  | 元数据、日志、产物      |
             +--------|--------+  +------------------------+
                      v
             +-----------------+
             | ppt-master Skill|
             | SVG → PPTX      |
             +-----------------+
```

| 库名 | 用途 | 为何优于替代方案 | 大致包体积 |
|---|---|---|---|
| React | 表单、任务状态与条件 UI | 状态模型清晰，AI 编码工具熟悉 | 未知 |
| Vite | 前端开发与构建 | 本地工具配置面小 | 未知 |
| Express | 本地 HTTP、SSE 与下载 | P0 API 简单，生态成熟 | 未知 |
| Zod | 输入和任务 JSON 校验 | schema 可复用在 API 与持久化边界 | 未知 |
| Node `child_process` | 启动 `codex exec` | 标准库，无额外进程依赖 | 0（标准库） |

最大风险是把 Codex JSONL 当成稳定业务协议。必须建立 CodexEventAdapter：原始事件全部落盘，业务层只消费少量已识别字段；未知事件被记录并忽略。

**可替换技术原则**：推荐 React/Vite + Express；若项目已有 Next.js，可用 Route Handler 替代 Express。不可变的是：浏览器不能直接执行 Codex；命令只能由本地服务在白名单目录启动；session/thread id 持久化后才能恢复。

## 第七章：交互细节

### 7.1 键盘快捷键

| 操作 | 快捷键 | 备注 |
|---|---|---|
| 提交当前确认 | Ctrl + Enter | 仅确认文本框聚焦时启用，提交前显示确认弹窗 |
| 返回任务列表 | Alt + Left | 沿用浏览器行为，不额外拦截 |

### 7.2 右键菜单与上下文菜单

P0 不自定义全局右键。产物卡片更多菜单：

```text
更多
+-- 下载文件
+-- 打开所在目录
+-- 复制文件路径
+-- 重新扫描产物
```

### 7.3 空状态

- 首页无任务：“上传材料，创建第一份 PPT”。
- 运行中无消息：“Codex 已启动，等待第一条事件”，不伪造进度。
- 无产物：“任务目录已保留，可继续任务或打开目录检查”。
- 环境失败：分别显示 Codex 与 Skill 状态和重新检查按钮。

### 7.4 错误状态

| 触发条件 | 用户可见的提示信息 | 恢复操作 |
|---|---|---|
| 找不到 Codex | 未检测到可用的 Codex CLI | 重新检查；展开路径说明 |
| 找不到 ppt-master | ppt-master Skill 不在配置路径 | 选择 Skill 路径或重新检查 |
| 上传失败 | 材料未保存，表单内容仍在 | 重新选择文件 |
| Codex 非零退出 | 生成中断，材料与日志已保留 | 查看末尾日志；重试或继续 |
| session 无法恢复 | 原任务无法直接继续 | 基于现有目录创建新任务 |

### 7.5 加载状态

- 环境检查超过 200ms 显示行内加载，超过 5 秒显示“检查时间较长”。
- 上传超过 200ms 显示文件级进度，超过 60 秒允许取消。
- Agent 运行始终显示阶段与已运行时长，不显示虚假百分比。
- 产物扫描超过 300ms 显示骨架列表，超过 5 秒显示重新扫描。

## 第八章：导出与输出系统

### 8.1 支持的输出格式

| 格式 | 使用场景 | 质量选项 | 备注 |
|---|---|---|---|
| PPTX | 最终演示与继续编辑 | 由 `ppt-master` 决定 | P0 成功必需 |
| PDF | 分享与固定版式 | 由 Skill 决定 | 可选，不作为成功条件 |
| SVG | 页面源文件与审计 | 原始 SVG | 作为目录产物 |
| Markdown | 讲稿、设计规范 | 原始文本 | 支持下载 |
| JSONL | 运行审计与排错 | 原始事件 | 不在主产物区突出 |

### 8.2 输出文件结构

```text
data/jobs/job_20260714_001/
+-- job.json
+-- input/resume-project.pdf
+-- logs/
|   +-- events.jsonl
|   +-- last-message.md
|   +-- phase-a-proposal.md
+-- project/
|   +-- spec_lock.md
|   +-- svg_output/
|   +-- notes/
+-- output/
    +-- AI产品经理项目复盘.pptx
    +-- AI产品经理项目复盘.pdf
```

### 8.3 批量处理流程

P0 不支持批量；多个任务进入单并发队列：

```text
任务 A（运行） → 完成/暂停 → 任务 B（运行） → 任务 C
```

材料复制可并行；同一个 `ppt-master` 任务内部必须遵守串行页面生成规则，产品不得并行化页面。

## 第九章：开发优先级

### P0 - 没有这个，产品根本无法使用。交付标准：功能可用，不需要完美。

- 环境检查：Codex、ppt-master 和可写任务目录。
- 新建任务与材料上传。
- 安全启动 `codex exec --json` 并保存完整事件。
- 识别并保存 session/thread id。
- 展示首次最后消息和明确确认入口。
- 使用 `codex exec resume` 继续同一任务。
- 扫描并下载 PPTX；无 PPTX 不得标记成功。
- 停止、失败、重试和保留任务目录。

### P1 - 没有这个，用户第一次体验后不会回来。交付标准：功能完整，体验有连续性。

- 任务历史与再次继续。
- 六阶段业务状态映射。
- ppt-master 本地模板列表与显式路径选择。
- 完成通知和用户可懂的错误说明。

### P2 - 有了这个，用户会把产品推荐给别人。交付标准：稳定且有辨识度。

- 任务复盘卡与运行时间统计。
- 一个经过许可和格式转换验证的 Open Design 模板适配器。
- PPT 页面缩略图预览。

### P3 - 有了这个，一部分用户会付费或强烈倡导。交付标准：精致且有完整文档。

- 多 Skill 协议与第三方 Skill 表单生成。
- 可选桌面打包、自动更新和跨设备任务同步。
- 团队模板库与组织级设计规范。

## 第十章：性能指标

| 指标名称 | 目标值 | 测量方法 | 劣化阈值 |
|---|---:|---|---:|
| 本地首页可交互时间 | ≤ 1200ms | Chrome Performance | > 2500ms |
| 环境检查返回时间 | ≤ 2000ms | API 计时 | > 5000ms |
| 事件显示延迟 | ≤ 300ms | JSONL 行到 SSE 渲染时间差 | > 1000ms |
| 刷新后任务状态恢复 | ≤ 800ms | 页面加载到 job.json 状态显示 | > 2000ms |
| 100MB 材料上传 | ≤ 30s | 本机 SSD 端到端计时 | > 60s |
| 产物扫描（1000 文件） | ≤ 1500ms | 扫描开始到清单返回 | > 5000ms |
| 未知 JSONL 事件导致崩溃 | 0 次 | 注入 20 条未知事件 | ≥ 1 次 |
| 成功状态误报 | 0% | 10 次核对 PPTX 存在且大小 > 0 | > 0% |

PPT 总生成时间受材料、页数、模型和串行规则影响，不设置虚假统一 SLA；产品展示真实已运行时长，30 分钟后提示可离开页面但不要终止本地服务。

## 第十一章：开发者交接说明

### a) 实现顺序建议

你先做技术探针而不是首页：启动 `codex exec --json`，保存 stdout/stderr，识别 session/thread id，再用 `codex exec resume` 成功恢复。探针跑通后，依次实现任务目录、Node API、SSE、确认页、产物扫描，最后做视觉包装。

### b) 最可能导致返工的三个决策

1. **JSONL 字段散落在 UI**：安全默认是集中到 CodexEventAdapter；出现新事件时只修改适配器。
2. **首次正常退出直接判完成**：安全默认是有有效 PPTX 才完成；有 session id 但无 PPTX 则等待确认或未完成。
3. **允许任意命令或目录**：安全默认是固定命令模板、固定 Skill、服务端生成 job id；P0 安全验收后再扩展。

### c) 哪里要严格，哪里可以灵活

- 第一章范围与硬约束：**约束**，不得加入 HTML 编辑、多 Agent 或账号。
- 第二、三章状态与确认流程：**约束**，这是核心产品合同。
- 第五章 status、session id、工作区和产物路径：**约束**；文件组织细节：**建议**。
- 第六章技术栈：**建议**；浏览器/本地服务/Codex 三层边界不可改变。
- 第七章视觉和微交互：**建议**，但不能隐藏失败和未知状态。
- 第八章 PPTX 成功判定：**约束**。
- 第九章 P0：**约束**，P1–P3 不得与 P0 同时开发。

### d) 已知的未知项

- 此处未解决：`codex exec --json` 的精确 session/thread id 事件字段，需要用已安装的 0.144.1 版本采样确认。
- 此处未解决：用户所说的“opencode 已证明需求”具体指 OpenCode 还是 Open Design；不影响单 Skill MVP，但影响竞品叙事。
- 此处未解决：哪些 Open Design 模板的许可证、结构和视觉资产适合转换为 `ppt-master` 模板；P0 不使用。
- 此处未解决：长任务中 Codex 会话失效后的自动恢复；P0 允许人工基于现有项目继续。
- 当前本机 `codex` 启动会出现临时目录权限警告，但版本和 `exec` 帮助可正常返回；实现时不能把警告误判为任务失败。

### e) 验收剧本

```text
验收剧本 1：在 Windows 本机打开环境检查页，应看到 Codex 版本和 ppt-master 路径；把 Skill 路径改为不存在后，应禁用“创建并开始分析”，用页面截图验证。

验收剧本 2：上传小型 Markdown 并创建任务，应在任务目录看到材料副本、job.json 和持续增长的 events.jsonl，用文件列表和日志行数验证。

验收剧本 3：首次 ppt-master 运行结束后，应进入“等待设计确认”，展示完整建议并保存非空 session/thread id，用 job.json 与页面截图验证。

验收剧本 4：提交“确认以上八项，继续生成”后，应调用 resume 而非新建任务，resumeCount 增加，原目录继续产生文件，用 session id 和修改时间验证。

验收剧本 5：只有发现大小大于 0 的 PPTX 才显示“生成完成”；删除测试 PPTX 后重新扫描，应回到“未完成”，用页面状态和产物索引验证。
```



