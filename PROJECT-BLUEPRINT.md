# DeckFlow Lite 快速交付路线

## 产品范围

- 平台：Windows 本地网页应用
- 执行引擎：本机 `codex exec`
- 首个 Skill：`ppt-master`
- 首个产物：PPTX 及其项目目录
- 目标：7–10 天做出稳定 Demo

## Day 1：跑通技术探针

- 用程序启动一次 `codex exec --json`。
- 从 JSONL 中识别 session/thread id、文本消息、完成和失败事件。
- 在测试目录创建一个小文件，证明 Codex 能在指定工作区执行。
- 用 `codex exec resume` 恢复同一任务。

验收：一个最小脚本可以启动、读取事件、暂停后恢复。

## Day 2–3：完成产品外壳

- 首页只放“创建 PPT”与最近任务。
- 表单支持上传材料、填写主题、目标受众和演示时长。
- 后端为每次任务创建独立、白名单范围内的项目目录。
- 运行页展示状态、最近消息和停止按钮。

验收：不调用 `ppt-master` 时，模拟任务可以完整走完页面状态。

## Day 4–5：接入 ppt-master 两阶段流程

- 第一阶段启动 Skill，停在八项确认。
- 页面展示 Codex 给出的完整建议。
- 用户点击“确认并继续”或输入修改意见。
- 后端使用 session/thread id 恢复同一任务。

验收：一次真实任务能从材料进入八项确认，再恢复生成。

## Day 6–7：产物与失败恢复

- 扫描任务目录中的 PPTX、PDF、SVG、Markdown 和日志。
- 提供打开产物目录、下载文件和再次继续任务。
- 正确处理 Codex 不存在、Skill 不存在、材料无法读取、任务异常退出。

验收：三个测试材料至少两个完整生成；失败任务能显示原因并重试。

## Day 8–10：面试包装

- 做 3 分钟演示视频。
- 编写 GitHub README、架构图和真实限制。
- 保存一次失败案例及修复前后对比。
- 只在主流程稳定后增加一个模板选择页面。

## Open Design 模板处理原则

Open Design 的 HTML deck 模板与 `ppt-master` 的 SVG/PPTX 模板结构不同，第一版不直接混用。后续先选择一个许可证明确的模板，做“模板元数据 + 预览图 + ppt-master 路径”的显式适配，成功后再扩展。
